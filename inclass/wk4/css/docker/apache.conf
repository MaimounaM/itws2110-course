# Static pages only this week: DocumentRoot is public/, every file is real.
ServerName localhost
<VirtualHost *:80>
    DocumentRoot /app/public
    <Directory /app/public>
        Require all granted
        Options +Indexes
    </Directory>
    ErrorLog  /dev/stderr
    CustomLog /dev/stdout combined
</VirtualHost>
