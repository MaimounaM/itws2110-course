// Adds the DOM matchers used below — toHaveTextContent, toHaveValue, toBeInTheDocument.
// Without this line those matchers do not exist and every drill fails on the matcher,
// not on your answer.
import '@testing-library/jest-dom/vitest';
